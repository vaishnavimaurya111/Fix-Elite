
  # On-Demand Home Services Platform

  This is a code bundle for On-Demand Home Services Platform. The original project is available at https://www.figma.com/design/F31LwCmjN2asn5Su01CbX8/On-Demand-Home-Services-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  